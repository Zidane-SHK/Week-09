Machine Learning -- Week 09, Ikram, Lect:
LO: Using Scikit, pandas, frames, plot, and previous knowledge to learn anc create foundation in ML
fit(): trains model
x: features/col
y: samples/rows
model.fit(x_train, y_train): supervised
model.predict(x_test): makes predictions based on features(x)
scaler.transform(x): convert/process data. scale, encode, extract features
^ Value - Mean / Std

Looking into KNN
